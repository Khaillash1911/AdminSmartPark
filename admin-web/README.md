# SmartPark admin web

This directory contains the Angular administration dashboard.

Use the repository-level [run guide](../docs/RUNNING.md) for installation, required backend services, ports, and troubleshooting.

From this directory:

```bash
npm install --legacy-peer-deps
npm start -- --port 4300
```

Production build:

```bash
npm run build
```
